import { getRegistryItem } from "@glinui/registry"
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("accordion")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Accordion",
  description: item?.description ?? "Expandable content sections with smooth disclosure behavior.",
  path: "/docs/components/accordion"
})

<ComponentDocLayout badgeLabel="Primitive / Molecule" componentId="accordion" implementation="radix" title="Accordion" description={item?.description ?? "Expandable content sections with smooth disclosure behavior."}>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add accordion"} />

  ## Usage

  <ExampleBlock code={`import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@glinui/ui"

export function AccordionBasicDemo() {
  return (
    <Accordion type="single" collapsible>
      <AccordionItem value="item-1">
        <AccordionTrigger>What is Glin UI?</AccordionTrigger>
        <AccordionContent>A liquid-glass component library for React.</AccordionContent>
      </AccordionItem>
    </Accordion>
  )
}`}> 
    <Accordion type="single" collapsible>
      <AccordionItem value="item-1">
        <AccordionTrigger>What is Glin UI?</AccordionTrigger>
        <AccordionContent>A liquid-glass component library for React.</AccordionContent>
      </AccordionItem>
    </Accordion>
  </ExampleBlock>

  ## Examples

  ### Glass Variant

  <ExampleBlock code={`import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@glinui/ui"

export function AccordionGlassDemo() {
  return (
    <Accordion type="single" collapsible>
      <AccordionItem value="one" variant="glass">
        <AccordionTrigger variant="glass">Setup</AccordionTrigger>
        <AccordionContent variant="glass">Install the package and import primitives.</AccordionContent>
      </AccordionItem>
      <AccordionItem value="two" variant="glass">
        <AccordionTrigger variant="glass">Theming</AccordionTrigger>
        <AccordionContent variant="glass">Use token variables for all surfaces.</AccordionContent>
      </AccordionItem>
    </Accordion>
  )
}`}>
    <Accordion type="single" collapsible>
      <AccordionItem value="one" variant="glass">
        <AccordionTrigger variant="glass">Setup</AccordionTrigger>
        <AccordionContent variant="glass">Install the package and import primitives.</AccordionContent>
      </AccordionItem>
      <AccordionItem value="two" variant="glass">
        <AccordionTrigger variant="glass">Theming</AccordionTrigger>
        <AccordionContent variant="glass">Use token variables for all surfaces.</AccordionContent>
      </AccordionItem>
    </Accordion>
  </ExampleBlock>

  ### Frosted Variant

  Heavier frost with stronger blur and an inner glow. Toggle backgrounds above to compare.

  <ExampleBlock code={`import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@glinui/ui"

export function AccordionFrostedDemo() {
  return (
    <Accordion type="single" collapsible>
      <AccordionItem value="one" variant="frosted">
        <AccordionTrigger variant="frosted">Installation</AccordionTrigger>
        <AccordionContent variant="frosted">Run pnpm add @glinui/ui @glinui/tokens to get started.</AccordionContent>
      </AccordionItem>
      <AccordionItem value="two" variant="frosted">
        <AccordionTrigger variant="frosted">Configuration</AccordionTrigger>
        <AccordionContent variant="frosted">Import the CSS tokens in your root layout file.</AccordionContent>
      </AccordionItem>
      <AccordionItem value="three" variant="frosted">
        <AccordionTrigger variant="frosted">Usage</AccordionTrigger>
        <AccordionContent variant="frosted">Import components and apply variant props.</AccordionContent>
      </AccordionItem>
    </Accordion>
  )
}`}>
    <Accordion type="single" collapsible>
      <AccordionItem value="one" variant="frosted">
        <AccordionTrigger variant="frosted">Installation</AccordionTrigger>
        <AccordionContent variant="frosted">Run pnpm add @glinui/ui @glinui/tokens to get started.</AccordionContent>
      </AccordionItem>
      <AccordionItem value="two" variant="frosted">
        <AccordionTrigger variant="frosted">Configuration</AccordionTrigger>
        <AccordionContent variant="frosted">Import the CSS tokens in your root layout file.</AccordionContent>
      </AccordionItem>
      <AccordionItem value="three" variant="frosted">
        <AccordionTrigger variant="frosted">Usage</AccordionTrigger>
        <AccordionContent variant="frosted">Import components and apply variant props.</AccordionContent>
      </AccordionItem>
    </Accordion>
  </ExampleBlock>

  ## Accessibility

  - Built on Radix Accordion with keyboard and ARIA semantics.
  - Use concise trigger labels.

  ## Reduced Motion

  Content transitions are reduced under `prefers-reduced-motion`.

  ## API Reference

  ### Accordion

  <PropsTable componentId="accordion" propsType="AccordionProps" autoAppendGenerated />

  ### AccordionItem

  <PropsTable componentId="accordion" propsType="AccordionItemProps" autoAppendGenerated />

  ### AccordionTrigger

  <PropsTable componentId="accordion" propsType="AccordionTriggerProps" autoAppendGenerated />

  ### AccordionContent

  <PropsTable componentId="accordion" propsType="AccordionContentProps" autoAppendGenerated />

  ## Source

  ```tsx
  import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@glinui/ui"
  ```
</ComponentDocLayout>
