import { getRegistryItem } from "@glinui/registry"
import { Badge } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("badge")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Badge",
  description: item?.description ?? "Compact status indicator for labels, states, and counts.",
  path: "/docs/components/badge"
})

<ComponentDocLayout badgeLabel="Primitive / Atom" componentId="badge" implementation="radix" title="Badge" description={item?.description ?? "Compact status indicator for labels, states, and counts."}>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add badge"} />

  ## Usage

  <ExampleBlock code={`import { Badge } from "@glinui/ui"

export function BadgeBasicDemo() {
  return <Badge>Status</Badge>
}`}> 
    <Badge>Status</Badge>
  </ExampleBlock>

  ## Examples

  ### Variants

  <ExampleBlock code={`import { Badge } from "@glinui/ui"

export function BadgeVariantDemo() {
  return (
    <div className="flex flex-wrap gap-2">
      <Badge>Default</Badge>
      <Badge variant="glass">Glass</Badge>
      <Badge variant="outline">Outline</Badge>
      <Badge variant="ghost">Ghost</Badge>
    </div>
  )
}`}> 
    <div className="flex flex-wrap gap-2">
      <Badge>Default</Badge>
      <Badge variant="glass">Glass</Badge>
      <Badge variant="outline">Outline</Badge>
      <Badge variant="ghost">Ghost</Badge>
    </div>
  </ExampleBlock>

  ### Sizes

  <ExampleBlock code={`import { Badge } from "@glinui/ui"

export function BadgeSizeDemo() {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <Badge size="sm">Small</Badge>
      <Badge size="md">Medium</Badge>
      <Badge size="lg">Large</Badge>
    </div>
  )
}`}> 
    <div className="flex flex-wrap items-center gap-2">
      <Badge size="sm">Small</Badge>
      <Badge size="md">Medium</Badge>
      <Badge size="lg">Large</Badge>
    </div>
  </ExampleBlock>

  ## Accessibility

  - Use badge text that remains understandable out of context.
  - Do not rely on color-only meaning.

  ## Reduced Motion

  Badge is static and motion-independent.

  ## API Reference

  <PropsTable componentId="badge" autoAppendGenerated />

  ## Source

  ```tsx
  import { Badge } from "@glinui/ui"
  ```
</ComponentDocLayout>
