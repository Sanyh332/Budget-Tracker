import { getRegistryItem } from "@glinui/registry"
import { BlurSpotlight } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("blur-spotlight")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Blur Spotlight",
  description: item?.description ?? "Cursor-following ambient glow effect for background layers.",
  path: "/docs/components/blur-spotlight"
})

<ComponentDocLayout
  badgeLabel="Signature / Molecule"
  componentId="blur-spotlight"
  implementation="css"
  title="Blur Spotlight"
  description={item?.description ?? "Cursor-following ambient glow effect for background layers."}
>
  ## Installation

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add blur-spotlight"} />

  ## Usage

  `BlurSpotlight` is a `"use client"` component. Place it inside a relative container. The glow follows the cursor within the parent element. Great for cards, hero sections, and interactive backgrounds.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`"use client"
import { BlurSpotlight } from "@glinui/ui"

export function BlurSpotlightDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950 dark:border-neutral-800">
      <BlurSpotlight />
      <span className="relative z-10 text-3xl font-bold text-white">Hover Me</span>
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950 dark:border-neutral-800">
      <BlurSpotlight />
      <span className="relative z-10 text-3xl font-bold text-white">Hover Me</span>
    </div>
  </ExampleBlock>

  ## Examples

  ### Custom Color and Size

  <ExampleBlock
    code={`"use client"
import { BlurSpotlight } from "@glinui/ui"

export function BlurSpotlightCustomDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950 dark:border-neutral-800">
      <BlurSpotlight color="#ec4899" size={400} intensity={0.6} />
      <span className="relative z-10 text-2xl font-bold text-white">Pink Glow</span>
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950 dark:border-neutral-800">
      <BlurSpotlight color="#ec4899" size={400} intensity={0.6} />
      <span className="relative z-10 text-2xl font-bold text-white">Pink Glow</span>
    </div>
  </ExampleBlock>

  ### Subtle Effect

  <ExampleBlock
    code={`"use client"
import { BlurSpotlight } from "@glinui/ui"

export function BlurSpotlightSubtleDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950 dark:border-neutral-800">
      <BlurSpotlight size={200} blur={120} intensity={0.3} />
      <span className="relative z-10 text-2xl font-bold text-white">Subtle</span>
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950 dark:border-neutral-800">
      <BlurSpotlight size={200} blur={120} intensity={0.3} />
      <span className="relative z-10 text-2xl font-bold text-white">Subtle</span>
    </div>
  </ExampleBlock>

  ## Accessibility

  - The component renders with `aria-hidden` — invisible to assistive technologies.
  - Purely decorative; carries no semantic meaning.
  - `pointer-events-none` ensures content beneath remains interactive.

  ## Reduced Motion

  The entire spotlight layer is hidden via `motion-reduce:hidden` when reduced motion is preferred.

  ## API Reference

  <PropsTable componentId="blur-spotlight" autoAppendGenerated />

  ## Source

  ```tsx
  import { BlurSpotlight } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
