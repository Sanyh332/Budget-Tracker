import { ArrowRight } from "lucide-react"
import { getRegistryItem } from "@glinui/registry"
import { Button } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("button")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Button",
  description: item?.description ?? "Action control with glass and matte-forward styling for light and dark modes.",
  path: "/docs/components/button"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="button"
  implementation="radix"
  title="Button"
  description={item?.description ?? "Action control with glass and matte-forward styling for light and dark modes."}
>
  ## Installation

  Install from the package for shared ownership or from the registry for copy-paste control.

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add button"} />

  ## Usage

  The Button component wraps the native `button` element with tokenized variants, sizes, and motion defaults.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { Button } from "@glinui/ui"

export function ButtonBasicDemo() {
  return <Button>Button</Button>
}`}
    snippets={[
      {
        label: "TSX",
        language: "tsx",
        code: `import { Button } from "@glinui/ui"

export function ButtonBasicDemo() {
  return <Button>Button</Button>
}`
      },
      {
        label: "JSX",
        language: "jsx",
        code: `import { Button } from "@glinui/ui"

export function ButtonBasicDemo() {
  return <Button>Button</Button>
}`
      }
    ]}
  >
    <Button>Button</Button>
  </ExampleBlock>

  ### Cursor

  <ExampleBlock
    code={`import { ArrowRight } from "lucide-react"
import { Button } from "@glinui/ui"

export function ButtonCursorDemo() {
  return (
    <div className="flex items-center gap-2">
      <Button variant="outline">Button</Button>
      <Button size="sm" variant="outline" className="h-9 w-9 px-0" aria-label="Submit">
        <ArrowRight className="size-4" />
      </Button>
    </div>
  )
}`}
  >
    <div className="flex items-center gap-2">
      <Button variant="outline">Button</Button>
      <Button size="sm" variant="outline" className="h-9 w-9 px-0" aria-label="Submit">
        <ArrowRight className="size-4" />
      </Button>
    </div>
  </ExampleBlock>

  ## Examples

  ### Size

  <ExampleBlock
    code={`import { Button } from "@glinui/ui"

export function ButtonSizeDemo() {
  return (
    <div className="flex items-end gap-2">
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </div>
  )
}`}
    snippets={[
      {
        label: "TSX RTL",
        language: "tsx",
        code: `import { Button } from "@glinui/ui"

export function ButtonRtlDemo() {
  return (
    <div dir="rtl" className="flex justify-center">
      <div className="flex items-center gap-2">
        <Button variant="glass">حفظ</Button>
        <Button variant="outline">إلغاء</Button>
      </div>
    </div>
  )
}`
      },
      {
        label: "JSX RTL",
        language: "jsx",
        code: `import { Button } from "@glinui/ui"

export function ButtonRtlDemo() {
  return (
    <div dir="rtl" className="flex justify-center">
      <div className="flex items-center gap-2">
        <Button variant="glass">حفظ</Button>
        <Button variant="outline">إلغاء</Button>
      </div>
    </div>
  )
}`
      }
    ]}
  >
    <div className="flex items-end gap-2">
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </div>
  </ExampleBlock>

  ### Variants

  Toggle backgrounds (top-right) to see how each variant adapts to different surfaces.

  <ExampleBlock
    code={`import { Button } from "@glinui/ui"

export function ButtonVariantDemo() {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <Button>Default</Button>
      <Button variant="glass">Glass</Button>
      <Button variant="frosted">Frosted</Button>
      <Button variant="liquid">Liquid</Button>
      <Button variant="matte">Matte</Button>
      <Button variant="glow">Glow</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="ghost">Ghost</Button>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-2">
      <Button>Default</Button>
      <Button variant="glass">Glass</Button>
      <Button variant="frosted">Frosted</Button>
      <Button variant="liquid">Liquid</Button>
      <Button variant="matte">Matte</Button>
      <Button variant="glow">Glow</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="ghost">Ghost</Button>
    </div>
  </ExampleBlock>

  ### With Icon

  <ExampleBlock
    code={`import { ArrowRight } from "lucide-react"
import { Button } from "@glinui/ui"

export function ButtonWithIconDemo() {
  return (
    <Button variant="liquid" className="gap-2">
      Continue
      <ArrowRight className="size-4" />
    </Button>
  )
}`}
  >
    <Button variant="liquid" className="gap-2">
      Continue
      <ArrowRight className="size-4" />
    </Button>
  </ExampleBlock>

  ### Button Group

  <ExampleBlock
    code={`import { Button } from "@glinui/ui"

export function ButtonGroupDemo() {
  return (
    <div className="inline-flex items-center gap-1 rounded-full border border-border/60 bg-background/45 p-1">
      <Button size="sm" className="rounded-full">Day</Button>
      <Button size="sm" variant="ghost" className="rounded-full">Week</Button>
      <Button size="sm" variant="ghost" className="rounded-full">Month</Button>
    </div>
  )
}`}
  >
    <div className="inline-flex items-center gap-1 rounded-full border border-border/60 bg-background/45 p-1">
      <Button size="sm" className="rounded-full">Day</Button>
      <Button size="sm" variant="ghost" className="rounded-full">Week</Button>
      <Button size="sm" variant="ghost" className="rounded-full">Month</Button>
    </div>
  </ExampleBlock>

  ### RTL

  Use `dir="rtl"` on a layout wrapper to mirror button flow for right-to-left locales.

  <ExampleBlock
    code={`import { Button } from "@glinui/ui"

export function ButtonRtlDemo() {
  return (
    <div dir="rtl" className="flex justify-center">
      <div className="flex items-center gap-2">
        <Button variant="glass">حفظ</Button>
        <Button variant="outline">إلغاء</Button>
      </div>
    </div>
  )
}`}
  >
    <div dir="rtl" className="flex justify-center">
      <div className="flex items-center gap-2">
        <Button variant="glass">حفظ</Button>
        <Button variant="outline">إلغاء</Button>
      </div>
    </div>
  </ExampleBlock>

  ### Internationalized Labels

  Keep labels outside the component and inject locale strings. Code blocks in docs stay `ltr` for readability.

  ```tsx
  import { Button } from "@glinui/ui"

  const copy = {
    en: { save: "Save", cancel: "Cancel" },
    ar: { save: "حفظ", cancel: "إلغاء" }
  }

  export function SaveActions({ locale = "en" }: { locale?: "en" | "ar" }) {
    const t = copy[locale]

    return (
      <div dir={locale === "ar" ? "rtl" : "ltr"} className="flex gap-2">
        <Button>{t.save}</Button>
        <Button variant="ghost">{t.cancel}</Button>
      </div>
    )
  }
  ```

  ## Accessibility

  - Native `button` semantics by default.
  - Visible focus ring via tokenized accent color.
  - `disabled` state blocks interaction and lowers visual emphasis.
  - Supports `asChild` for semantic link rendering when needed.

  ## Reduced Motion

  Button transitions use tokenized timing and disable transform-heavy motion when users enable
  `prefers-reduced-motion`.

  ## API Reference

  The `Button` component is a wrapper around the native button element that adds variant styling,
  size control, and optional slot composition.

  <PropsTable componentId="button" autoAppendGenerated />

  ### Source

  Import from `@glinui/ui`.

  ```tsx
  import { Button } from "@glinui/ui"
  ```

  Source files:
  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
