import { getRegistryItem } from "@glinui/registry"
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("card")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Card",
  description: item?.description ?? "Flexible surface container for grouped content.",
  path: "/docs/components/card"
})

<ComponentDocLayout badgeLabel="Primitive / Molecule" componentId="card" implementation="radix" title="Card" description={item?.description ?? "Flexible surface container for grouped content."}>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add card"} />

  ## Usage

  <ExampleBlock code={`import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from "@glinui/ui"

export function CardBasicDemo() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Project overview</CardTitle>
        <CardDescription>Weekly summary.</CardDescription>
      </CardHeader>
      <CardContent>Revenue increased by 18%.</CardContent>
      <CardFooter>Updated now</CardFooter>
    </Card>
  )
}`}> 
    <Card>
      <CardHeader>
        <CardTitle>Project overview</CardTitle>
        <CardDescription>Weekly summary.</CardDescription>
      </CardHeader>
      <CardContent>Revenue increased by 18%.</CardContent>
      <CardFooter>Updated now</CardFooter>
    </Card>
  </ExampleBlock>

  ## Examples

  ### Variants

  Use the background toggle (top-right) to test each variant against different surfaces.

  <ExampleBlock code={`import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@glinui/ui"

export function CardVariantDemo() {
  return (
    <div className="grid gap-3 md:grid-cols-2">
      <Card variant="glass"><CardHeader><CardTitle>Glass</CardTitle></CardHeader><CardContent>Standard translucent surface with subtle refraction.</CardContent></Card>
      <Card variant="frosted"><CardHeader><CardTitle>Frosted</CardTitle></CardHeader><CardContent>Heavy frost with stronger blur and inner glow.</CardContent></Card>
      <Card variant="liquid"><CardHeader><CardTitle>Liquid</CardTitle></CardHeader><CardContent>Wet-glass effect with radial highlights.</CardContent></Card>
      <Card variant="matte"><CardHeader><CardTitle>Matte</CardTitle></CardHeader><CardContent>Solid opaque surface with soft shadow.</CardContent></Card>
    </div>
  )
}`}>
    <div className="grid gap-3 md:grid-cols-2">
      <Card variant="glass"><CardHeader><CardTitle>Glass</CardTitle></CardHeader><CardContent>Standard translucent surface with subtle refraction.</CardContent></Card>
      <Card variant="frosted"><CardHeader><CardTitle>Frosted</CardTitle></CardHeader><CardContent>Heavy frost with stronger blur and inner glow.</CardContent></Card>
      <Card variant="liquid"><CardHeader><CardTitle>Liquid</CardTitle></CardHeader><CardContent>Wet-glass effect with radial highlights.</CardContent></Card>
      <Card variant="matte"><CardHeader><CardTitle>Matte</CardTitle></CardHeader><CardContent>Solid opaque surface with soft shadow.</CardContent></Card>
    </div>
  </ExampleBlock>

  ### Frosted on Vivid Background

  The frosted variant shines against colorful backgrounds where the blur and inner glow create visible depth.

  <ExampleBlock code={`import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from "@glinui/ui"

export function CardFrostedDemo() {
  return (
    <Card variant="frosted" className="max-w-sm">
      <CardHeader>
        <CardTitle>Analytics</CardTitle>
        <CardDescription>Real-time dashboard metrics.</CardDescription>
      </CardHeader>
      <CardContent>
        <div className="text-3xl font-bold tracking-tight">12,847</div>
        <p className="text-sm text-neutral-600 dark:text-neutral-300">+23% from last week</p>
      </CardContent>
      <CardFooter className="text-xs text-neutral-500">Updated 2 min ago</CardFooter>
    </Card>
  )
}`}>
    <Card variant="frosted" className="max-w-sm">
      <CardHeader>
        <CardTitle>Analytics</CardTitle>
        <CardDescription>Real-time dashboard metrics.</CardDescription>
      </CardHeader>
      <CardContent>
        <div className="text-3xl font-bold tracking-tight">12,847</div>
        <p className="text-sm text-neutral-600 dark:text-neutral-300">+23% from last week</p>
      </CardContent>
      <CardFooter className="text-xs text-neutral-500">Updated 2 min ago</CardFooter>
    </Card>
  </ExampleBlock>

  ## Accessibility

  - Card uses semantic section content you provide.
  - Prefer heading levels that match page hierarchy.

  ## Reduced Motion

  Card supports reduced-motion fallback for transition-heavy states.

  ## API Reference

  ### Card

  <PropsTable componentId="card" propsType="CardProps" autoAppendGenerated />

  ### CardHeader

  <PropsTable componentId="card" propsType="CardHeaderProps" autoAppendGenerated />

  ### CardTitle

  <PropsTable componentId="card" propsType="CardTitleProps" autoAppendGenerated />

  ### CardDescription

  <PropsTable componentId="card" propsType="CardDescriptionProps" autoAppendGenerated />

  ### CardContent

  <PropsTable componentId="card" propsType="CardContentProps" autoAppendGenerated />

  ### CardFooter

  <PropsTable componentId="card" propsType="CardFooterProps" autoAppendGenerated />

  ## Source

  ```tsx
  import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from "@glinui/ui"
  ```
</ComponentDocLayout>
