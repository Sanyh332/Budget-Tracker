import { getRegistryItem } from "@glinui/registry"
import { Checkbox } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("checkbox")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Checkbox",
  description: item?.description ?? "Radix checkbox with glass, liquid, matte, and outline surface variants.",
  path: "/docs/components/checkbox"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="checkbox"
  implementation="radix"
  title="Checkbox"
  description={item?.description ?? "Radix checkbox with glass, liquid, matte, and outline surface variants."}
>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add checkbox"} />

  ## Usage

  <ExampleBlock
    code={`import { Checkbox } from "@glinui/ui"

export function CheckboxBasicDemo() {
  return (
    <div className="flex items-center gap-2">
      <Checkbox id="basic" />
      <label htmlFor="basic" className="text-sm">Accept terms and conditions</label>
    </div>
  )
}`}
  >
    <div className="flex items-center gap-2">
      <Checkbox id="basic" />
      <label htmlFor="basic" className="text-sm">Accept terms and conditions</label>
    </div>
  </ExampleBlock>

  ## Examples

  ### Checked by Default

  <ExampleBlock
    code={`import { Checkbox } from "@glinui/ui"

export function CheckboxDefaultCheckedDemo() {
  return (
    <div className="flex items-center gap-2">
      <Checkbox id="checked" defaultChecked />
      <label htmlFor="checked" className="text-sm">Subscribed to newsletter</label>
    </div>
  )
}`}
  >
    <div className="flex items-center gap-2">
      <Checkbox id="checked" defaultChecked />
      <label htmlFor="checked" className="text-sm">Subscribed to newsletter</label>
    </div>
  </ExampleBlock>

  ### Surface Variants

  <ExampleBlock
    code={`import { Checkbox } from "@glinui/ui"

export function CheckboxVariantsDemo() {
  return (
    <div className="flex flex-wrap items-center gap-3">
      <Checkbox variant="default" defaultChecked aria-label="Default" />
      <Checkbox variant="glass" defaultChecked aria-label="Glass" />
      <Checkbox variant="liquid" defaultChecked aria-label="Liquid" />
      <Checkbox variant="matte" defaultChecked aria-label="Matte" />
      <Checkbox variant="outline" defaultChecked aria-label="Outline" />
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-3">
      <Checkbox variant="default" defaultChecked aria-label="Default" />
      <Checkbox variant="glass" defaultChecked aria-label="Glass" />
      <Checkbox variant="liquid" defaultChecked aria-label="Liquid" />
      <Checkbox variant="matte" defaultChecked aria-label="Matte" />
      <Checkbox variant="outline" defaultChecked aria-label="Outline" />
    </div>
  </ExampleBlock>

  ### Sizes

  <ExampleBlock
    code={`import { Checkbox } from "@glinui/ui"

export function CheckboxSizesDemo() {
  return (
    <div className="flex flex-wrap items-center gap-4">
      <Checkbox size="sm" defaultChecked aria-label="Small checkbox" />
      <Checkbox size="md" defaultChecked aria-label="Medium checkbox" />
      <Checkbox size="lg" defaultChecked aria-label="Large checkbox" />
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-4">
      <Checkbox size="sm" defaultChecked aria-label="Small checkbox" />
      <Checkbox size="md" defaultChecked aria-label="Medium checkbox" />
      <Checkbox size="lg" defaultChecked aria-label="Large checkbox" />
    </div>
  </ExampleBlock>

  ### Disabled

  <ExampleBlock
    code={`import { Checkbox } from "@glinui/ui"

export function CheckboxDisabledDemo() {
  return (
    <div className="space-y-2">
      <div className="flex items-center gap-2">
        <Checkbox id="dis-unchecked" disabled />
        <label htmlFor="dis-unchecked" className="text-sm">Disabled unchecked</label>
      </div>
      <div className="flex items-center gap-2">
        <Checkbox id="dis-checked" disabled defaultChecked />
        <label htmlFor="dis-checked" className="text-sm">Disabled checked</label>
      </div>
    </div>
  )
}`}
  >
    <div className="space-y-2">
      <div className="flex items-center gap-2">
        <Checkbox id="dis-unchecked" disabled />
        <label htmlFor="dis-unchecked" className="text-sm">Disabled unchecked</label>
      </div>
      <div className="flex items-center gap-2">
        <Checkbox id="dis-checked" disabled defaultChecked />
        <label htmlFor="dis-checked" className="text-sm">Disabled checked</label>
      </div>
    </div>
  </ExampleBlock>

  ### With Form Label

  <ExampleBlock
    code={`import { Checkbox } from "@glinui/ui"

export function CheckboxFormLabelDemo() {
  return (
    <div className="space-y-2">
      <div className="flex items-center gap-2">
        <Checkbox id="terms" required name="terms" />
        <label htmlFor="terms" className="text-sm">I agree to the terms of service</label>
      </div>
      <div className="flex items-center gap-2">
        <Checkbox id="privacy" name="privacy" />
        <label htmlFor="privacy" className="text-sm">I agree to the privacy policy</label>
      </div>
    </div>
  )
}`}
  >
    <div className="space-y-2">
      <div className="flex items-center gap-2">
        <Checkbox id="terms" required name="terms" />
        <label htmlFor="terms" className="text-sm">I agree to the terms of service</label>
      </div>
      <div className="flex items-center gap-2">
        <Checkbox id="privacy" name="privacy" />
        <label htmlFor="privacy" className="text-sm">I agree to the privacy policy</label>
      </div>
    </div>
  </ExampleBlock>

  ## Accessibility

  - Built on `@radix-ui/react-checkbox`, which renders a fully accessible checkbox with keyboard support.
  - Always pair with a `<label>` via `htmlFor` / `id` or wrap both in a `<label>` element.
  - The `required` prop sets `aria-required` on the underlying button element.
  - Space toggles the checkbox; Enter is not handled by default (browser behavior).

  ## Reduced Motion

  The check indicator uses a brief scale/fade transition that is suppressed when `prefers-reduced-motion: reduce` is active.

  ## API Reference

  <PropsTable componentId="checkbox" autoAppendGenerated />

  ## Source

  ```tsx
  import { Checkbox } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
