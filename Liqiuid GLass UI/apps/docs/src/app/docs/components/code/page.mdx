import { getRegistryItem } from "@glinui/registry"
import { Code } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("code")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Code",
  description: item?.description ?? "Inline code element for commands, snippets, and tokenized literals.",
  path: "/docs/components/code"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="code"
  implementation="radix"
  title="Code"
  description={item?.description ?? "Inline code element for commands, snippets, and tokenized literals."}
>
  ## Installation

  Install from the package for shared ownership or from the registry for copy-paste control.

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add code"} />

  ## Usage

  `Code` renders a semantic `<code>` element with a monospace font. Use it inline inside prose for commands, API values, variable names, and short code snippets. For multi-line blocks, pair it with a dedicated code block component.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { Code } from "@glinui/ui"

export function CodeBasicDemo() {
  return <Code>npm install @glinui/ui</Code>
}`}
  >
    <Code>npm install @glinui/ui</Code>
  </ExampleBlock>

  ## Examples

  ### Variants

  The `variant` prop adjusts the surface behind the code token — filled, glass-blurred, border-only, or fully transparent.

  <ExampleBlock
    code={`import { Code } from "@glinui/ui"

export function CodeVariantDemo() {
  return (
    <div className="flex flex-wrap gap-3">
      <Code variant="default">default</Code>
      <Code variant="glass">glass</Code>
      <Code variant="outline">outline</Code>
      <Code variant="ghost">ghost</Code>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap gap-3">
      <Code variant="default">default</Code>
      <Code variant="glass">glass</Code>
      <Code variant="outline">outline</Code>
      <Code variant="ghost">ghost</Code>
    </div>
  </ExampleBlock>

  ### Sizes

  Scale the monospace font with `size`. All three sizes remain optically balanced against body text at the corresponding text scale.

  <ExampleBlock
    code={`import { Code } from "@glinui/ui"

export function CodeSizeDemo() {
  return (
    <div className="flex flex-wrap items-center gap-3">
      <Code size="sm">sm code</Code>
      <Code size="md">md code</Code>
      <Code size="lg">lg code</Code>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-3">
      <Code size="sm">sm code</Code>
      <Code size="md">md code</Code>
      <Code size="lg">lg code</Code>
    </div>
  </ExampleBlock>

  ### Inline in Paragraph

  `Code` is designed to sit inside flowing text without disrupting line height. Drop it directly into a `<p>` element alongside prose.

  <ExampleBlock
    code={`import { Code } from "@glinui/ui"

export function CodeInlineDemo() {
  return (
    <p className="text-sm leading-relaxed text-neutral-700 dark:text-neutral-300">
      Run <Code>pnpm dev</Code> to start the development server, then open{" "}
      <Code>http://localhost:3000</Code> in your browser. Set the{" "}
      <Code>NODE_ENV</Code> environment variable to <Code>production</Code>{" "}
      before deploying.
    </p>
  )
}`}
  >
    <p className="text-sm leading-relaxed text-neutral-700 dark:text-neutral-300">
      Run <Code>pnpm dev</Code> to start the development server, then open{" "}
      <Code>http://localhost:3000</Code> in your browser. Set the{" "}
      <Code>NODE_ENV</Code> environment variable to <Code>production</Code>{" "}
      before deploying.
    </p>
  </ExampleBlock>

  ### Glass Variant in Context

  The `glass` variant pairs well with translucent surfaces like cards or overlays.

  <ExampleBlock
    code={`import { Code } from "@glinui/ui"

export function CodeGlassDemo() {
  return (
    <p className="text-sm text-neutral-700 dark:text-neutral-300">
      Import the hook with{" "}
      <Code variant="glass">import {"{ useLiquidGlass }"} from "@glinui/ui"</Code>
      {" "}and attach the returned <Code variant="glass">ref</Code> to your element.
    </p>
  )
}`}
  >
    <p className="text-sm text-neutral-700 dark:text-neutral-300">
      Import the hook with{" "}
      <Code variant="glass">{'import { useLiquidGlass } from "@glinui/ui"'}</Code>
      {" "}and attach the returned <Code variant="glass">ref</Code> to your element.
    </p>
  </ExampleBlock>

  ## Accessibility

  - `Code` renders a native `<code>` element, which assistive technologies announce as a code region.
  - Screen readers may vocalize code content differently — keep snippets short and purposeful.
  - Do not use `Code` for decorative styling; only wrap content that is genuinely code or a technical literal.
  - Ensure sufficient contrast between the code token and the surrounding prose in all variants.

  ## Reduced Motion

  `Code` is a static inline element with no animation. No reduced-motion considerations apply.

  ## API Reference

  <PropsTable componentId="code" autoAppendGenerated />

  ## Source

  ```tsx
  import { Code } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
