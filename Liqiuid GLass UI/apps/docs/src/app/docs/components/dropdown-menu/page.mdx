import { getRegistryItem } from "@glinui/registry"
import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("dropdown-menu")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Dropdown Menu",
  description: item?.description ?? "Action menu anchored to a trigger with keyboard navigation.",
  path: "/docs/components/dropdown-menu"
})

<ComponentDocLayout badgeLabel="Primitive / Molecule" componentId="dropdown-menu" implementation="radix" title="Dropdown Menu" description={item?.description ?? "Action menu anchored to a trigger with keyboard navigation."}>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add dropdown-menu"} />

  ## Usage

  <ExampleBlock code={`import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem } from "@glinui/ui"

export function DropdownBasicDemo() {
  return (
    <DropdownMenu>
      <DropdownMenuTrigger>Open menu</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem>Profile</DropdownMenuItem>
        <DropdownMenuItem>Billing</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  )
}`}> 
    <DropdownMenu>
      <DropdownMenuTrigger>Open menu</DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem>Profile</DropdownMenuItem>
        <DropdownMenuItem>Billing</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  </ExampleBlock>

  ## Examples

  ### Glass

  <ExampleBlock code={`import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator } from "@glinui/ui"

export function DropdownGlassDemo() {
  return (
    <DropdownMenu>
      <DropdownMenuTrigger variant="glass">Actions</DropdownMenuTrigger>
      <DropdownMenuContent variant="glass">
        <DropdownMenuItem>Rename</DropdownMenuItem>
        <DropdownMenuItem>Duplicate</DropdownMenuItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem>Archive</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  )
}`}> 
    <DropdownMenu>
      <DropdownMenuTrigger variant="glass">Actions</DropdownMenuTrigger>
      <DropdownMenuContent variant="glass">
        <DropdownMenuItem>Rename</DropdownMenuItem>
        <DropdownMenuItem>Duplicate</DropdownMenuItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem>Archive</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  </ExampleBlock>

  ## Accessibility

  - Built on Radix Dropdown Menu.
  - Supports keyboard navigation, escape, and focus restore.

  ## Reduced Motion

  Menu open/close should remain readable without animation.

  ## API Reference

  ### DropdownMenuTrigger

  <PropsTable componentId="dropdown-menu" propsType="DropdownMenuTriggerProps" autoAppendGenerated />

  ### DropdownMenuContent

  <PropsTable componentId="dropdown-menu" propsType="DropdownMenuContentProps" autoAppendGenerated />

  ### DropdownMenuItem

  <PropsTable componentId="dropdown-menu" propsType="DropdownMenuItemProps" autoAppendGenerated />

  ## Source

  ```tsx
  import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem } from "@glinui/ui"
  ```
</ComponentDocLayout>
