import { getRegistryItem } from "@glinui/registry"
import { IconFrame } from "@glinui/ui"
import { Sparkles, Zap } from "lucide-react"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("icon-frame")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "IconFrame",
  description: item?.description ?? "Consistent framed surface for icon glyphs and short initials.",
  path: "/docs/components/icon-frame"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="icon-frame"
  implementation="radix"
  title="IconFrame"
  description={item?.description ?? "Consistent framed surface for icon glyphs and short initials."}
>
  ## Installation

  Install from the package for shared ownership or from the registry for copy-paste control.

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add icon-frame"} />

  ## Usage

  `IconFrame` wraps any children — a Lucide icon, a text initial, or an image — inside a fixed-size square with `rounded-xl` corners and `shrink-0` to prevent layout squishing. It is the building block for feature lists, avatars, app icons, and notification items.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { IconFrame } from "@glinui/ui"
import { Sparkles } from "lucide-react"

export function IconFrameBasicDemo() {
  return (
    <IconFrame>
      <Sparkles className="h-5 w-5" />
    </IconFrame>
  )
}`}
  >
    <IconFrame>
      <Sparkles className="h-5 w-5" />
    </IconFrame>
  </ExampleBlock>

  ## Examples

  ### Variants

  Choose a surface treatment to match the surrounding context — filled for feature highlights, glass for overlay panels, outline for list rows, ghost for minimal layouts.

  <ExampleBlock
    code={`import { IconFrame } from "@glinui/ui"
import { Sparkles } from "lucide-react"

export function IconFrameVariantDemo() {
  return (
    <div className="flex flex-wrap items-center gap-3">
      <IconFrame variant="default">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
      <IconFrame variant="glass">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
      <IconFrame variant="outline">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
      <IconFrame variant="ghost">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-3">
      <IconFrame variant="default">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
      <IconFrame variant="glass">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
      <IconFrame variant="outline">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
      <IconFrame variant="ghost">
        <Sparkles className="h-5 w-5" />
      </IconFrame>
    </div>
  </ExampleBlock>

  ### Sizes

  The `size` prop controls fixed square dimensions. All three sizes use `rounded-xl` corners and auto-center children.

  <ExampleBlock
    code={`import { IconFrame } from "@glinui/ui"
import { Zap } from "lucide-react"

export function IconFrameSizeDemo() {
  return (
    <div className="flex flex-wrap items-center gap-3">
      <IconFrame size="sm">
        <Zap className="h-3.5 w-3.5" />
      </IconFrame>
      <IconFrame size="md">
        <Zap className="h-5 w-5" />
      </IconFrame>
      <IconFrame size="lg">
        <Zap className="h-7 w-7" />
      </IconFrame>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-3">
      <IconFrame size="sm">
        <Zap className="h-3.5 w-3.5" />
      </IconFrame>
      <IconFrame size="md">
        <Zap className="h-5 w-5" />
      </IconFrame>
      <IconFrame size="lg">
        <Zap className="h-7 w-7" />
      </IconFrame>
    </div>
  </ExampleBlock>

  ### With Initials

  Pass short text children instead of an icon for product logos, user avatars, or app abbreviations.

  <ExampleBlock
    code={`import { IconFrame } from "@glinui/ui"

export function IconFrameInitialsDemo() {
  return (
    <div className="flex flex-wrap items-center gap-3">
      <IconFrame variant="default" size="md">GU</IconFrame>
      <IconFrame variant="glass" size="md">AB</IconFrame>
      <IconFrame variant="outline" size="md">JD</IconFrame>
      <IconFrame variant="ghost" size="md">MK</IconFrame>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-3">
      <IconFrame variant="default" size="md">GU</IconFrame>
      <IconFrame variant="glass" size="md">AB</IconFrame>
      <IconFrame variant="outline" size="md">JD</IconFrame>
      <IconFrame variant="ghost" size="md">MK</IconFrame>
    </div>
  </ExampleBlock>

  ### Feature List Pattern

  Combine `IconFrame` with a heading and description to build a feature grid. The consistent square dimensions keep rows optically aligned.

  <ExampleBlock
    code={`import { IconFrame } from "@glinui/ui"
import { Sparkles, Zap } from "lucide-react"

export function IconFrameFeatureDemo() {
  return (
    <div className="space-y-4 max-w-sm">
      <div className="flex items-start gap-3">
        <IconFrame variant="glass" size="md">
          <Sparkles className="h-5 w-5" />
        </IconFrame>
        <div>
          <p className="text-sm font-medium">Liquid Glass</p>
          <p className="text-xs text-neutral-500">SVG displacement refraction effects.</p>
        </div>
      </div>
      <div className="flex items-start gap-3">
        <IconFrame variant="glass" size="md">
          <Zap className="h-5 w-5" />
        </IconFrame>
        <div>
          <p className="text-sm font-medium">Fast by Default</p>
          <p className="text-xs text-neutral-500">Zero runtime overhead for static tokens.</p>
        </div>
      </div>
    </div>
  )
}`}
  >
    <div className="space-y-4 max-w-sm">
      <div className="flex items-start gap-3">
        <IconFrame variant="glass" size="md">
          <Sparkles className="h-5 w-5" />
        </IconFrame>
        <div>
          <p className="text-sm font-medium">Liquid Glass</p>
          <p className="text-xs text-neutral-500">SVG displacement refraction effects.</p>
        </div>
      </div>
      <div className="flex items-start gap-3">
        <IconFrame variant="glass" size="md">
          <Zap className="h-5 w-5" />
        </IconFrame>
        <div>
          <p className="text-sm font-medium">Fast by Default</p>
          <p className="text-xs text-neutral-500">Zero runtime overhead for static tokens.</p>
        </div>
      </div>
    </div>
  </ExampleBlock>

  ## Accessibility

  - `IconFrame` is a decorative container. Icons inside it should have `aria-hidden="true"` when accompanied by visible text labels.
  - When used as a standalone icon button (e.g., wrapped in a `<button>`), provide an `aria-label` on the button element.
  - Text initials inside `IconFrame` should be supplemented with a surrounding `aria-label` or `title` for screen readers when used as an avatar.
  - The frame itself carries no interactive semantics — wrap it in a `<button>` or `<a>` when interaction is needed.

  ## Reduced Motion

  `IconFrame` is a static layout primitive with no animation. No reduced-motion considerations apply.

  ## API Reference

  <PropsTable componentId="icon-frame" autoAppendGenerated />

  ## Source

  ```tsx
  import { IconFrame } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
