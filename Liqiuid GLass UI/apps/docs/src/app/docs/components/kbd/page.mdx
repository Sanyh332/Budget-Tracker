import { getRegistryItem } from "@glinui/registry"
import { Kbd } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("kbd")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Kbd",
  description: item?.description ?? "Keyboard keycap primitive for shortcuts and key hints.",
  path: "/docs/components/kbd"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="kbd"
  implementation="radix"
  title="Kbd"
  description={item?.description ?? "Keyboard keycap primitive for shortcuts and key hints."}
>
  ## Installation

  Install from the package for shared ownership or from the registry for copy-paste control.

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add kbd"} />

  ## Usage

  `Kbd` renders a semantic `<kbd>` element styled as a keyboard key cap. It uses uppercase tracking, a monospace font, and a minimum width of `min-w-6` to ensure single-character keys remain square. Use it to communicate keyboard shortcuts in documentation, tooltips, or UI hints.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { Kbd } from "@glinui/ui"

export function KbdBasicDemo() {
  return <Kbd>⌘</Kbd>
}`}
  >
    <Kbd>⌘</Kbd>
  </ExampleBlock>

  ## Examples

  ### Shortcuts

  Combine multiple `Kbd` elements with a `+` separator to represent compound key shortcuts.

  <ExampleBlock
    code={`import { Kbd } from "@glinui/ui"

export function KbdShortcutDemo() {
  return (
    <div className="flex items-center gap-1">
      <Kbd>⌘</Kbd>
      <span className="text-neutral-400">+</span>
      <Kbd>K</Kbd>
    </div>
  )
}`}
  >
    <div className="flex items-center gap-1">
      <Kbd>⌘</Kbd>
      <span className="text-neutral-400">+</span>
      <Kbd>K</Kbd>
    </div>
  </ExampleBlock>

  ### Variants

  <ExampleBlock
    code={`import { Kbd } from "@glinui/ui"

export function KbdVariantDemo() {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <Kbd variant="default">⌘</Kbd>
      <Kbd variant="glass">⌘</Kbd>
      <Kbd variant="outline">⌘</Kbd>
      <Kbd variant="ghost">⌘</Kbd>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-2">
      <Kbd variant="default">⌘</Kbd>
      <Kbd variant="glass">⌘</Kbd>
      <Kbd variant="outline">⌘</Kbd>
      <Kbd variant="ghost">⌘</Kbd>
    </div>
  </ExampleBlock>

  ### Sizes

  <ExampleBlock
    code={`import { Kbd } from "@glinui/ui"

export function KbdSizeDemo() {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <Kbd size="sm">⌘</Kbd>
      <Kbd size="md">⌘</Kbd>
      <Kbd size="lg">⌘</Kbd>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap items-center gap-2">
      <Kbd size="sm">⌘</Kbd>
      <Kbd size="md">⌘</Kbd>
      <Kbd size="lg">⌘</Kbd>
    </div>
  </ExampleBlock>

  ### Multi-Key Sequences

  String full keyboard sequences for step-by-step instructions.

  <ExampleBlock
    code={`import { Kbd } from "@glinui/ui"

export function KbdSequenceDemo() {
  return (
    <div className="flex items-center gap-1">
      <Kbd>⌃</Kbd>
      <span className="text-neutral-400">+</span>
      <Kbd>⇧</Kbd>
      <span className="text-neutral-400">+</span>
      <Kbd>P</Kbd>
    </div>
  )
}`}
  >
    <div className="flex items-center gap-1">
      <Kbd>⌃</Kbd>
      <span className="text-neutral-400">+</span>
      <Kbd>⇧</Kbd>
      <span className="text-neutral-400">+</span>
      <Kbd>P</Kbd>
    </div>
  </ExampleBlock>

  ## Accessibility

  - `Kbd` renders a native `<kbd>` element which is recognized by assistive technologies as denoting keyboard input.
  - Screen readers will announce the content of each `<kbd>` element; use descriptive key names or Unicode symbols that read naturally.
  - Avoid using `<Kbd>` purely for styling — reserve it for actual keyboard key references.

  ## Reduced Motion

  `Kbd` is a static element with no animation. It is unaffected by `prefers-reduced-motion`.

  ## API Reference

  <PropsTable componentId="kbd" autoAppendGenerated />

  ## Source

  ```tsx
  import { Kbd } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
