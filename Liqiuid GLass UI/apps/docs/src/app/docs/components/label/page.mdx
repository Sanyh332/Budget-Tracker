import { getRegistryItem } from "@glinui/registry"
import { Label, Input, Checkbox } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("label")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Label",
  description: item?.description ?? "Form label primitive with shared glass-aware variants.",
  path: "/docs/components/label"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="label"
  implementation="radix"
  title="Label"
  description={item?.description ?? "Form label primitive with shared glass-aware variants."}
>
  ## Installation

  Install from the package for shared ownership or from the registry for copy-paste control.

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add label"} />

  ## Usage

  `Label` renders a semantic `<label>` element. Associate it with a form control via the `htmlFor` prop, which maps directly to the HTML `for` attribute. All glass-aware variant surfaces are shared with the other atom primitives.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { Label, Input } from "@glinui/ui"

export function LabelBasicDemo() {
  return (
    <div className="flex flex-col gap-1.5">
      <Label htmlFor="email">Email address</Label>
      <Input id="email" type="email" placeholder="you@example.com" />
    </div>
  )
}`}
  >
    <div className="flex flex-col gap-1.5">
      <Label htmlFor="email">Email address</Label>
      <Input id="email" type="email" placeholder="you@example.com" />
    </div>
  </ExampleBlock>

  ## Examples

  ### Variants

  <ExampleBlock
    code={`import { Label } from "@glinui/ui"

export function LabelVariantDemo() {
  return (
    <div className="flex flex-col gap-2">
      <Label variant="default">Default label</Label>
      <Label variant="glass">Glass label</Label>
      <Label variant="outline">Outline label</Label>
      <Label variant="ghost">Ghost label</Label>
    </div>
  )
}`}
  >
    <div className="flex flex-col gap-2">
      <Label variant="default">Default label</Label>
      <Label variant="glass">Glass label</Label>
      <Label variant="outline">Outline label</Label>
      <Label variant="ghost">Ghost label</Label>
    </div>
  </ExampleBlock>

  ### Sizes

  <ExampleBlock
    code={`import { Label } from "@glinui/ui"

export function LabelSizeDemo() {
  return (
    <div className="flex flex-col gap-2">
      <Label size="sm">Small label</Label>
      <Label size="md">Medium label</Label>
      <Label size="lg">Large label</Label>
    </div>
  )
}`}
  >
    <div className="flex flex-col gap-2">
      <Label size="sm">Small label</Label>
      <Label size="md">Medium label</Label>
      <Label size="lg">Large label</Label>
    </div>
  </ExampleBlock>

  ### With Checkbox

  <ExampleBlock
    code={`import { Label, Checkbox } from "@glinui/ui"

export function LabelCheckboxDemo() {
  return (
    <div className="flex items-center gap-2">
      <Checkbox id="terms" />
      <Label htmlFor="terms">I agree to the terms and conditions</Label>
    </div>
  )
}`}
  >
    <div className="flex items-center gap-2">
      <Checkbox id="terms" />
      <Label htmlFor="terms">I agree to the terms and conditions</Label>
    </div>
  </ExampleBlock>

  ### Required Pattern

  Use an asterisk span inside `Label` to signal required fields. Pair with `aria-required` on the control.

  <ExampleBlock
    code={`import { Label, Input } from "@glinui/ui"

export function LabelRequiredDemo() {
  return (
    <div className="flex flex-col gap-1.5">
      <Label htmlFor="username">
        Username
        <span className="ml-1 text-red-500" aria-hidden="true">*</span>
      </Label>
      <Input id="username" aria-required="true" placeholder="johndoe" />
    </div>
  )
}`}
  >
    <div className="flex flex-col gap-1.5">
      <Label htmlFor="username">
        Username
        <span className="ml-1 text-red-500" aria-hidden="true">*</span>
      </Label>
      <Input id="username" aria-required="true" placeholder="johndoe" />
    </div>
  </ExampleBlock>

  ## Accessibility

  - Always provide `htmlFor` to associate the label with its form control — clicking the label should move focus to the input.
  - Do not use `Label` as decorative text; prefer `Text` or plain `<span>` elements for non-form contexts.
  - When marking required fields, include both the visual asterisk and an `aria-required="true"` attribute on the control.

  ## Reduced Motion

  `Label` is a static element with no animation. It is unaffected by `prefers-reduced-motion`.

  ## API Reference

  <PropsTable componentId="label" autoAppendGenerated />

  ## Source

  ```tsx
  import { Label } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
