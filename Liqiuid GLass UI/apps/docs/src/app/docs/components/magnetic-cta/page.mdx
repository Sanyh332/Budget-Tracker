import { getRegistryItem } from "@glinui/registry"
import { MagneticCTA } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("magnetic-cta")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Magnetic CTA",
  description: item?.description ?? "Call-to-action button with subtle cursor-attraction on hover.",
  path: "/docs/components/magnetic-cta"
})

<ComponentDocLayout
  badgeLabel="Signature / Atom"
  componentId="magnetic-cta"
  implementation="css"
  title="Magnetic CTA"
  description={item?.description ?? "Call-to-action button with subtle cursor-attraction on hover."}
>
  ## Installation

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add magnetic-cta"} />

  ## Usage

  `MagneticCTA` wraps a `Button` and applies a pointer-driven CSS `translate` offset whenever the cursor enters the magnetic radius. The button itself remains fully accessible — the attraction is a decorative layer applied to the wrapper.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { MagneticCTA } from "@glinui/ui"

export function MagneticCTADemo() {
  return <MagneticCTA variant="glass">Book a demo</MagneticCTA>
}`}
  >
    <MagneticCTA variant="glass">Book a demo</MagneticCTA>
  </ExampleBlock>

  ## Examples

  ### Custom Offset and Radius

  Increase `maxOffset` for a stronger pull and `magnetRadius` to activate the effect from further away.

  <ExampleBlock
    code={`import { MagneticCTA } from "@glinui/ui"

export function MagneticCTAStrongDemo() {
  return (
    <div className="flex flex-wrap gap-6">
      <MagneticCTA variant="liquid" maxOffset={14} magnetRadius={180}>
        Strong pull
      </MagneticCTA>
      <MagneticCTA variant="glass" maxOffset={4} magnetRadius={80}>
        Subtle pull
      </MagneticCTA>
    </div>
  )
}`}
  >
    <div className="flex flex-wrap gap-6">
      <MagneticCTA variant="liquid" maxOffset={14} magnetRadius={180}>
        Strong pull
      </MagneticCTA>
      <MagneticCTA variant="glass" maxOffset={4} magnetRadius={80}>
        Subtle pull
      </MagneticCTA>
    </div>
  </ExampleBlock>

  ### Container Class

  Use `containerClassName` to control the layout or dimensions of the magnetic wrapper element.

  <ExampleBlock
    code={`import { MagneticCTA } from "@glinui/ui"

export function MagneticCTAContainerDemo() {
  return (
    <MagneticCTA
      variant="liquid"
      containerClassName="flex justify-center w-full"
    >
      Centered CTA
    </MagneticCTA>
  )
}`}
  >
    <MagneticCTA
      variant="liquid"
      containerClassName="flex justify-center w-full"
    >
      Centered CTA
    </MagneticCTA>
  </ExampleBlock>

  ## Accessibility

  - The magnetic cursor-follow effect is purely decorative and applied to an outer wrapper `<div>`.
  - The inner `<button>` element retains all native semantics, focus management, and keyboard interactions.
  - The focus ring is rendered on the button element and is not obscured by the transform offset.
  - Screen readers receive no indication of the motion layer.

  ## Reduced Motion

  When `prefers-reduced-motion` is active, the magnetic translation is reset to `translate(0, 0)` and the transition is removed entirely.

  ## API Reference

  <PropsTable componentId="magnetic-cta" autoAppendGenerated />

  ## Source

  ```tsx
  import { MagneticCTA } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
