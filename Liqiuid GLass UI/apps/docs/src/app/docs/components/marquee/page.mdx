import { getRegistryItem } from "@glinui/registry"
import { Marquee, Badge } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("marquee")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Marquee",
  description: item?.description ?? "Infinite scrolling content strip.",
  path: "/docs/components/marquee"
})

<ComponentDocLayout
  badgeLabel="Signature / Molecule"
  componentId="marquee"
  implementation="css"
  title="Marquee"
  description={item?.description ?? "Infinite scrolling content strip."}
>
  ## Installation

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add marquee"} />

  ## Usage

  Place any elements as children. The marquee duplicates them to create a seamless infinite scroll loop.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { Marquee, Badge } from "@glinui/ui"

export function MarqueeDemo() {
  return (
    <Marquee className="py-4" gap={24}>
      {["React", "Next.js", "Tailwind", "Radix", "TypeScript", "Vitest"].map((t) => (
        <Badge key={t} variant="glass" className="mx-2 text-sm">{t}</Badge>
      ))}
    </Marquee>
  )
}`}
  >
    <Marquee className="py-4" gap={24}>
      {["React", "Next.js", "Tailwind", "Radix", "TypeScript", "Vitest"].map((t) => (
        <Badge key={t} variant="glass" className="mx-2 text-sm">{t}</Badge>
      ))}
    </Marquee>
  </ExampleBlock>

  ## Examples

  ### Vertical

  <ExampleBlock
    code={`import { Marquee, Badge } from "@glinui/ui"

export function MarqueeVerticalDemo() {
  return (
    <div className="h-48 overflow-hidden">
      <Marquee direction="up" speed={20} gap={16}>
        {["Alpha", "Beta", "Gamma", "Delta", "Epsilon"].map((t) => (
          <Badge key={t} variant="outline" className="my-1">{t}</Badge>
        ))}
      </Marquee>
    </div>
  )
}`}
  >
    <div className="h-48 overflow-hidden">
      <Marquee direction="up" speed={20} gap={16}>
        {["Alpha", "Beta", "Gamma", "Delta", "Epsilon"].map((t) => (
          <Badge key={t} variant="outline" className="my-1">{t}</Badge>
        ))}
      </Marquee>
    </div>
  </ExampleBlock>

  ### Pause on Hover

  <ExampleBlock
    code={`import { Marquee, Badge } from "@glinui/ui"

export function MarqueePauseDemo() {
  return (
    <Marquee pauseOnHover speed={15} gap={24}>
      {["Hover", "to", "pause", "the", "scroll"].map((t) => (
        <Badge key={t} className="mx-2">{t}</Badge>
      ))}
    </Marquee>
  )
}`}
  >
    <Marquee pauseOnHover speed={15} gap={24}>
      {["Hover", "to", "pause", "the", "scroll"].map((t) => (
        <Badge key={t} className="mx-2">{t}</Badge>
      ))}
    </Marquee>
  </ExampleBlock>

  ## Accessibility

  - The duplicated children are marked `aria-hidden` to prevent screen readers from reading content twice.
  - Avoid placing interactive elements inside the marquee.

  ## Reduced Motion

  Animation uses `motion-safe:animate-marquee-x/y`. Under `prefers-reduced-motion`, the content stays static.

  ## API Reference

  <PropsTable componentId="marquee" autoAppendGenerated />

  ## Source

  ```tsx
  import { Marquee } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
