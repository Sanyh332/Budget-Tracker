import { getRegistryItem } from "@glinui/registry"
import { MeteorShower } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("meteor-shower")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Meteor Shower",
  description: item?.description ?? "Falling diagonal streaks for hero backgrounds.",
  path: "/docs/components/meteor-shower"
})

<ComponentDocLayout
  badgeLabel="Signature / Molecule"
  componentId="meteor-shower"
  implementation="css"
  title="Meteor Shower"
  description={item?.description ?? "Falling diagonal streaks for hero backgrounds."}
>
  ## Installation

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add meteor-shower"} />

  ## Usage

  Place inside a relative container. Meteors fall diagonally with staggered delays and randomized positions.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { MeteorShower } from "@glinui/ui"

export function MeteorShowerDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950">
      <span className="z-10 text-sm font-medium text-white">Meteor Shower</span>
      <MeteorShower />
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-neutral-950">
      <span className="z-10 text-sm font-medium text-white">Meteor Shower</span>
      <MeteorShower />
    </div>
  </ExampleBlock>

  ## Examples

  ### Dense

  <ExampleBlock
    code={`import { MeteorShower } from "@glinui/ui"

export function MeteorShowerDenseDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl bg-neutral-950">
      <span className="z-10 text-sm font-medium text-white">Dense</span>
      <MeteorShower count={24} minDuration={1} maxDuration={3} />
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl bg-neutral-950">
      <span className="z-10 text-sm font-medium text-white">Dense</span>
      <MeteorShower count={24} minDuration={1} maxDuration={3} />
    </div>
  </ExampleBlock>

  ## Accessibility

  - Decorative (`aria-hidden`) — invisible to screen readers.
  - No interactive elements.

  ## Reduced Motion

  Component returns `null` when `prefers-reduced-motion` is active.

  ## API Reference

  <PropsTable componentId="meteor-shower" autoAppendGenerated />

  ## Source

  ```tsx
  import { MeteorShower } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
