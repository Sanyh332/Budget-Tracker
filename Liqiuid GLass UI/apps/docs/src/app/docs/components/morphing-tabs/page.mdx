import { getRegistryItem } from "@glinui/registry"
import { MorphingTabs } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("morphing-tabs")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Morphing Tabs",
  description: item?.description ?? "Tab component with a smoothly sliding indicator that morphs between items.",
  path: "/docs/components/morphing-tabs"
})

<ComponentDocLayout
  badgeLabel="Signature / Molecule"
  componentId="morphing-tabs"
  implementation="css"
  title="Morphing Tabs"
  description={item?.description ?? "Tab component with a smoothly sliding indicator that morphs between items."}
>
  ## Installation

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add morphing-tabs"} />

  ## Usage

  `MorphingTabs` is a `"use client"` component. Pass an array of `items` with `id` and `label`. The active indicator smoothly slides between tabs.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`"use client"
import { MorphingTabs } from "@glinui/ui"

export function MorphingTabsDemo() {
  return (
    <div className="flex items-center justify-center p-8">
      <MorphingTabs
        items={[
          { id: "overview", label: "Overview" },
          { id: "features", label: "Features" },
          { id: "pricing", label: "Pricing" },
          { id: "docs", label: "Docs" }
        ]}
      />
    </div>
  )
}`}
  >
    <div className="flex items-center justify-center p-8">
      <MorphingTabs
        items={[
          { id: "overview", label: "Overview" },
          { id: "features", label: "Features" },
          { id: "pricing", label: "Pricing" },
          { id: "docs", label: "Docs" }
        ]}
      />
    </div>
  </ExampleBlock>

  ## Examples

  ### Underline Variant

  <ExampleBlock
    code={`"use client"
import { MorphingTabs } from "@glinui/ui"

export function MorphingTabsUnderlineDemo() {
  return (
    <div className="flex items-center justify-center p-8">
      <MorphingTabs
        variant="underline"
        items={[
          { id: "all", label: "All" },
          { id: "active", label: "Active" },
          { id: "archived", label: "Archived" }
        ]}
      />
    </div>
  )
}`}
  >
    <div className="flex items-center justify-center p-8">
      <MorphingTabs
        variant="underline"
        items={[
          { id: "all", label: "All" },
          { id: "active", label: "Active" },
          { id: "archived", label: "Archived" }
        ]}
      />
    </div>
  </ExampleBlock>

  ### Solid Variant

  <ExampleBlock
    code={`"use client"
import { MorphingTabs } from "@glinui/ui"

export function MorphingTabsSolidDemo() {
  return (
    <div className="flex items-center justify-center p-8">
      <MorphingTabs
        variant="solid"
        size="lg"
        items={[
          { id: "daily", label: "Daily" },
          { id: "weekly", label: "Weekly" },
          { id: "monthly", label: "Monthly" }
        ]}
      />
    </div>
  )
}`}
  >
    <div className="flex items-center justify-center p-8">
      <MorphingTabs
        variant="solid"
        size="lg"
        items={[
          { id: "daily", label: "Daily" },
          { id: "weekly", label: "Weekly" },
          { id: "monthly", label: "Monthly" }
        ]}
      />
    </div>
  </ExampleBlock>

  ## Accessibility

  - Renders with `role="tablist"` on the container and `role="tab"` on each button.
  - `aria-selected` is set on the active tab.
  - Disabled tabs receive `disabled` attribute and are visually marked.

  ## Reduced Motion

  When `prefers-reduced-motion: reduce` is active, the indicator transition duration is set to `0` for instant position changes.

  ## API Reference

  <PropsTable componentId="morphing-tabs" autoAppendGenerated />

  ## Source

  ```tsx
  import { MorphingTabs } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
