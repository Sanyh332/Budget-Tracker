import { getRegistryItem } from "@glinui/registry"
import { Popover, PopoverTrigger, PopoverContent } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("popover")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Popover",
  description: item?.description ?? "Floating content panel anchored to a trigger element.",
  path: "/docs/components/popover"
})

<ComponentDocLayout badgeLabel="Primitive / Molecule" componentId="popover" implementation="radix" title="Popover" description={item?.description ?? "Floating content panel anchored to a trigger element."}>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add popover"} />

  ## Usage

  <ExampleBlock code={`import { Popover, PopoverTrigger, PopoverContent } from "@glinui/ui"

export function PopoverBasicDemo() {
  return (
    <Popover>
      <PopoverTrigger>Open popover</PopoverTrigger>
      <PopoverContent>Popover content</PopoverContent>
    </Popover>
  )
}`}> 
    <Popover>
      <PopoverTrigger>Open popover</PopoverTrigger>
      <PopoverContent>Popover content</PopoverContent>
    </Popover>
  </ExampleBlock>

  ## Examples

  ### Glass

  <ExampleBlock code={`import { Popover, PopoverTrigger, PopoverContent } from "@glinui/ui"

export function PopoverGlassDemo() {
  return (
    <Popover>
      <PopoverTrigger variant="glass">Filter</PopoverTrigger>
      <PopoverContent variant="glass" className="space-y-2">
        <p className="text-sm">Filter options</p>
      </PopoverContent>
    </Popover>
  )
}`}> 
    <Popover>
      <PopoverTrigger variant="glass">Filter</PopoverTrigger>
      <PopoverContent variant="glass" className="space-y-2">
        <p className="text-sm">Filter options</p>
      </PopoverContent>
    </Popover>
  </ExampleBlock>

  ## Accessibility

  - Built on Radix Popover for focus management and escape handling.
  - Keep popover content concise and task-focused.

  ## Reduced Motion

  Popover remains functional and readable with reduced motion settings.

  ## API Reference

  ### PopoverTrigger

  <PropsTable componentId="popover" propsType="PopoverTriggerProps" autoAppendGenerated />

  ### PopoverContent

  <PropsTable componentId="popover" propsType="PopoverContentProps" autoAppendGenerated />

  ## Source

  ```tsx
  import { Popover, PopoverTrigger, PopoverContent } from "@glinui/ui"
  ```
</ComponentDocLayout>
