import { getRegistryItem } from "@glinui/registry"
import { RetroGrid } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("retro-grid")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Retro Grid",
  description: item?.description ?? "CSS perspective grid background with repeating gradients.",
  path: "/docs/components/retro-grid"
})

<ComponentDocLayout
  badgeLabel="Signature / Molecule"
  componentId="retro-grid"
  implementation="css"
  title="Retro Grid"
  description={item?.description ?? "CSS perspective grid background with repeating gradients."}
>
  ## Installation

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add retro-grid"} />

  ## Usage

  Place inside a relative container with `overflow-hidden`. The grid uses CSS `perspective` with `repeating-linear-gradient` to create a vanishing-point tile effect.

  ### Basic

  <ExampleBlock
    codeDefaultOpen
    code={`import { RetroGrid } from "@glinui/ui"

export function RetroGridDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-white dark:border-neutral-800 dark:bg-neutral-950">
      <span className="relative z-10 text-3xl font-bold">Retro Grid</span>
      <RetroGrid />
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-white dark:border-neutral-800 dark:bg-neutral-950">
      <span className="relative z-10 text-3xl font-bold">Retro Grid</span>
      <RetroGrid />
    </div>
  </ExampleBlock>

  ## Examples

  ### Custom Angle

  <ExampleBlock
    code={`import { RetroGrid } from "@glinui/ui"

export function RetroGridAngleDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-white dark:border-neutral-800 dark:bg-neutral-950">
      <span className="relative z-10 text-3xl font-bold">45°</span>
      <RetroGrid angle={45} />
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-white dark:border-neutral-800 dark:bg-neutral-950">
      <span className="relative z-10 text-3xl font-bold">45°</span>
      <RetroGrid angle={45} />
    </div>
  </ExampleBlock>

  ### Custom Color and Cell Size

  <ExampleBlock
    code={`import { RetroGrid } from "@glinui/ui"

export function RetroGridColorDemo() {
  return (
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-indigo-950">
      <span className="relative z-10 text-2xl font-bold text-indigo-200">Custom Grid</span>
      <RetroGrid lineColor="#818cf8" lineOpacity={0.5} cellSize={80} />
    </div>
  )
}`}
  >
    <div className="relative flex h-64 w-full items-center justify-center overflow-hidden rounded-xl border border-neutral-200 bg-indigo-950">
      <span className="relative z-10 text-2xl font-bold text-indigo-200">Custom Grid</span>
      <RetroGrid lineColor="#818cf8" lineOpacity={0.5} cellSize={80} />
    </div>
  </ExampleBlock>

  ## Accessibility

  - The grid element is marked `aria-hidden="true"` — invisible to assistive technologies.
  - Purely decorative; carries no semantic meaning.

  ## Reduced Motion

  The perspective scroll animation is paused via `motion-reduce:animate-none` and `motion-reduce:[animation-play-state:paused]`. The static grid remains visible so the layout is unaffected.

  ## API Reference

  <PropsTable componentId="retro-grid" autoAppendGenerated />

  ## Source

  ```tsx
  import { RetroGrid } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
