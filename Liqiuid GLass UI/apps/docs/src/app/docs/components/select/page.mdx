import { getRegistryItem } from "@glinui/registry"
import { Select } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("select")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Select",
  description: item?.description ?? "Native select with glass, liquid, matte, and utility variants.",
  path: "/docs/components/select"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="select"
  title="Select"
  description={item?.description ?? "Native select with glass, liquid, matte, and utility variants."}
>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add select"} />

  ## Usage

  <ExampleBlock
    code={`import { Select } from "@glinui/ui"

export function SelectBasicDemo() {
  return (
    <Select
      aria-label="Framework"
      options={[
        { label: "React", value: "react" },
        { label: "Vue", value: "vue" },
        { label: "Svelte", value: "svelte" }
      ]}
      placeholder="Pick a framework"
    />
  )
}`}
  >
    <Select
      aria-label="Framework"
      options={[
        { label: "React", value: "react" },
        { label: "Vue", value: "vue" },
        { label: "Svelte", value: "svelte" }
      ]}
      placeholder="Pick a framework"
    />
  </ExampleBlock>

  ## Examples

  ### Sizes

  <ExampleBlock
    code={`import { Select } from "@glinui/ui"

const opts = [
  { label: "Alpha", value: "alpha" },
  { label: "Beta", value: "beta" },
  { label: "Gamma", value: "gamma" }
]

export function SelectSizesDemo() {
  return (
    <div className="space-y-3">
      <Select size="sm" aria-label="Small size select" options={opts} placeholder="Small" />
      <Select size="md" aria-label="Medium size select" options={opts} placeholder="Medium" />
      <Select size="lg" aria-label="Large size select" options={opts} placeholder="Large" />
    </div>
  )
}`}
  >
    <div className="space-y-3">
      <Select size="sm" aria-label="Small size select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Small" />
      <Select size="md" aria-label="Medium size select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Medium" />
      <Select size="lg" aria-label="Large size select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Large" />
    </div>
  </ExampleBlock>

  ### Surface Variants

  <ExampleBlock
    code={`import { Select } from "@glinui/ui"

const options = [
  { label: "Alpha", value: "alpha" },
  { label: "Beta", value: "beta" }
]

export function SelectVariantsDemo() {
  return (
    <div className="space-y-3">
      <Select variant="default" aria-label="Default select" options={options} placeholder="Default" />
      <Select variant="glass" aria-label="Glass select" options={options} placeholder="Glass" />
      <Select variant="liquid" aria-label="Liquid select" options={options} placeholder="Liquid" />
      <Select variant="matte" aria-label="Matte select" options={options} placeholder="Matte" />
      <Select variant="outline" aria-label="Outline select" options={options} placeholder="Outline" />
      <Select variant="ghost" aria-label="Ghost select" options={options} placeholder="Ghost" />
    </div>
  )
}`}
  >
    <div className="space-y-3">
      <Select variant="default" aria-label="Default select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Default" />
      <Select variant="glass" aria-label="Glass select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Glass" />
      <Select variant="liquid" aria-label="Liquid select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Liquid" />
      <Select variant="matte" aria-label="Matte select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Matte" />
      <Select variant="outline" aria-label="Outline select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Outline" />
      <Select variant="ghost" aria-label="Ghost select" options={[{ label: "Alpha", value: "alpha" }, { label: "Beta", value: "beta" }]} placeholder="Ghost" />
    </div>
  </ExampleBlock>

  ### With Placeholder

  <ExampleBlock
    code={`import { Select } from "@glinui/ui"

export function SelectPlaceholderDemo() {
  return (
    <Select
      aria-label="Placeholder select"
      options={[{ label: "Option A", value: "a" }, { label: "Option B", value: "b" }]}
      placeholder="Choose an option..."
    />
  )
}`}
  >
    <Select
      aria-label="Placeholder select"
      options={[{ label: "Option A", value: "a" }, { label: "Option B", value: "b" }]}
      placeholder="Choose an option..."
    />
  </ExampleBlock>

  ### Disabled Option

  <ExampleBlock
    code={`import { Select } from "@glinui/ui"

export function SelectDisabledOptionDemo() {
  return (
    <Select
      aria-label="Status select"
      options={[
        { label: "Available", value: "available" },
        { label: "Unavailable", value: "unavailable", disabled: true },
        { label: "Also available", value: "also" }
      ]}
      placeholder="Select status"
    />
  )
}`}
  >
    <Select
      aria-label="Status select"
      options={[
        { label: "Available", value: "available" },
        { label: "Unavailable", value: "unavailable", disabled: true },
        { label: "Also available", value: "also" }
      ]}
      placeholder="Select status"
    />
  </ExampleBlock>

  ### Disabled Select

  <ExampleBlock
    code={`import { Select } from "@glinui/ui"

export function SelectDisabledDemo() {
  return (
    <Select
      aria-label="Disabled select"
      disabled
      options={[{ label: "React", value: "react" }]}
      placeholder="Disabled select"
    />
  )
}`}
  >
    <Select
      aria-label="Disabled select"
      disabled
      options={[{ label: "React", value: "react" }]}
      placeholder="Disabled select"
    />
  </ExampleBlock>

  ## Accessibility

  - Renders a native `<select>` element for maximum browser and assistive technology compatibility.
  - Pair with a `<label>` using `htmlFor` or provide an `aria-label` when no visible label exists.
  - Placeholder text should not be used as the sole accessible label.
  - Disabled individual options use the native `disabled` attribute on `<option>` elements.

  ## Reduced Motion

  Select does not use custom animations; open/close behavior is controlled by the browser's native dropdown.

  ## API Reference

  <PropsTable componentId="select" autoAppendGenerated />

  ## Source

  ```tsx
  import { Select } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
