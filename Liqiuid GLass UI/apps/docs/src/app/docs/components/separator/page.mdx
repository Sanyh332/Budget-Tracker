import { getRegistryItem } from "@glinui/registry"
import { Separator } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("separator")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Separator",
  description: item?.description ?? "Visual divider for grouping content with orientation support.",
  path: "/docs/components/separator"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="separator"
  implementation="custom"
  title="Separator"
  description={item?.description ?? "Visual divider for grouping content with orientation support."}
>
  ## Installation

  Install from the package for shared ownership or from the registry for copy-paste control.

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add separator"} />

  ## Usage

  Drop a `Separator` between content sections to create visual hierarchy. Horizontal is the default; use `orientation="vertical"` inside flex containers.

  <ExampleBlock
    codeDefaultOpen
    code={`import { Separator } from "@glinui/ui"

export function SeparatorBasicDemo() {
  return (
    <div className="space-y-3">
      <p className="text-sm">Section A</p>
      <Separator />
      <p className="text-sm">Section B</p>
    </div>
  )
}`}
  >
    <div className="space-y-3">
      <p className="text-sm">Section A</p>
      <Separator />
      <p className="text-sm">Section B</p>
    </div>
  </ExampleBlock>

  ## Examples

  ### Variants

  <ExampleBlock
    code={`import { Separator } from "@glinui/ui"

export function SeparatorVariantDemo() {
  return (
    <div className="space-y-4">
      <Separator variant="default" />
      <Separator variant="glass" />
      <Separator variant="outline" />
      <Separator variant="ghost" />
    </div>
  )
}`}
  >
    <div className="space-y-4">
      <Separator variant="default" />
      <Separator variant="glass" />
      <Separator variant="outline" />
      <Separator variant="ghost" />
    </div>
  </ExampleBlock>

  ### Vertical

  Render inside a flex container with an explicit height to display a vertical divider.

  <ExampleBlock
    code={`import { Separator } from "@glinui/ui"

export function SeparatorVerticalDemo() {
  return (
    <div className="flex h-8 items-center gap-4">
      <span className="text-sm">Left</span>
      <Separator orientation="vertical" />
      <span className="text-sm">Right</span>
    </div>
  )
}`}
  >
    <div className="flex h-8 items-center gap-4">
      <span className="text-sm">Left</span>
      <Separator orientation="vertical" />
      <span className="text-sm">Right</span>
    </div>
  </ExampleBlock>

  ### Sizes

  <ExampleBlock
    code={`import { Separator } from "@glinui/ui"

export function SeparatorSizeDemo() {
  return (
    <div className="space-y-4">
      <Separator size="sm" />
      <Separator size="md" />
      <Separator size="lg" />
    </div>
  )
}`}
  >
    <div className="space-y-4">
      <Separator size="sm" />
      <Separator size="md" />
      <Separator size="lg" />
    </div>
  </ExampleBlock>

  ### Decorative vs Semantic

  By default separators are decorative (`aria-hidden`). Set `decorative={false}` for separators that carry structural meaning for assistive technology.

  <ExampleBlock
    code={`import { Separator } from "@glinui/ui"

export function SeparatorDecorativeDemo() {
  return (
    <div className="space-y-4">
      <div className="space-y-2">
        <p className="text-xs text-neutral-500 dark:text-neutral-400">decorative (default)</p>
        <Separator decorative />
      </div>
      <div className="space-y-2">
        <p className="text-xs text-neutral-500 dark:text-neutral-400">semantic</p>
        <Separator decorative={false} />
      </div>
    </div>
  )
}`}
  >
    <div className="space-y-4">
      <div className="space-y-2">
        <p className="text-xs text-neutral-500 dark:text-neutral-400">decorative (default)</p>
        <Separator decorative />
      </div>
      <div className="space-y-2">
        <p className="text-xs text-neutral-500 dark:text-neutral-400">semantic</p>
        <Separator decorative={false} />
      </div>
    </div>
  </ExampleBlock>

  ## Accessibility

  - Decorative separators default to `aria-hidden="true"`.
  - Use `decorative={false}` for separators that convey structural meaning.
  - Use `orientation="vertical"` for column-like content splits.

  ## Reduced Motion

  Separator is a static element and does not use motion.

  ## API Reference

  <PropsTable componentId="separator" autoAppendGenerated />

  ### Source

  ```tsx
  import { Separator } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
