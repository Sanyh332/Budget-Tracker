import { getRegistryItem } from "@glinui/registry"
import { Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("sheet")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Sheet",
  description: item?.description ?? "Sliding panel for side, top, or bottom contextual workflows.",
  path: "/docs/components/sheet"
})

<ComponentDocLayout badgeLabel="Primitive / Molecule" componentId="sheet" implementation="radix" title="Sheet" description={item?.description ?? "Sliding panel for side, top, or bottom contextual workflows."}>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add sheet"} />

  ## Usage

  <ExampleBlock code={`import { Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@glinui/ui"

export function SheetBasicDemo() {
  return (
    <Sheet>
      <SheetTrigger>Open sheet</SheetTrigger>
      <SheetContent>
        <SheetHeader>
          <SheetTitle>Project settings</SheetTitle>
          <SheetDescription>Adjust workspace preferences.</SheetDescription>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  )
}`}> 
    <Sheet>
      <SheetTrigger>Open sheet</SheetTrigger>
      <SheetContent>
        <SheetHeader>
          <SheetTitle>Project settings</SheetTitle>
          <SheetDescription>Adjust workspace preferences.</SheetDescription>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  </ExampleBlock>

  ## Examples

  ### Glass Side Sheet

  <ExampleBlock code={`import { Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@glinui/ui"

export function SheetGlassDemo() {
  return (
    <Sheet>
      <SheetTrigger variant="glass">Open settings</SheetTrigger>
      <SheetContent variant="glass" side="right">
        <SheetHeader>
          <SheetTitle>Workspace</SheetTitle>
          <SheetDescription>Manage theme and team defaults.</SheetDescription>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  )
}`}> 
    <Sheet>
      <SheetTrigger variant="glass">Open settings</SheetTrigger>
      <SheetContent variant="glass" side="right">
        <SheetHeader>
          <SheetTitle>Workspace</SheetTitle>
          <SheetDescription>Manage theme and team defaults.</SheetDescription>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  </ExampleBlock>

  ## Accessibility

  - Built on Radix Dialog primitives.
  - Focus is trapped while open and restored on close.

  ## Reduced Motion

  Slide transitions respect reduced-motion preferences.

  ## API Reference

  ### SheetTrigger

  <PropsTable componentId="sheet" propsType="SheetTriggerProps" autoAppendGenerated />

  ### SheetContent

  <PropsTable componentId="sheet" propsType="SheetContentProps" autoAppendGenerated />

  ## Source

  ```tsx
  import { Sheet, SheetTrigger, SheetContent } from "@glinui/ui"
  ```
</ComponentDocLayout>
