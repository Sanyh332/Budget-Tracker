import { getRegistryItem } from "@glinui/registry"
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("tabs")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Tabs",
  description: item?.description ?? "Tabbed interface for switching between grouped content panels.",
  path: "/docs/components/tabs"
})

<ComponentDocLayout badgeLabel="Primitive / Molecule" componentId="tabs" implementation="radix" title="Tabs" description={item?.description ?? "Tabbed interface for switching between grouped content panels."}>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "npm install @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add tabs"} />

  ## Usage

  <ExampleBlock code={`import { Tabs, TabsList, TabsTrigger, TabsContent } from "@glinui/ui"

export function TabsBasicDemo() {
  return (
    <Tabs defaultValue="overview">
      <TabsList>
        <TabsTrigger value="overview">Overview</TabsTrigger>
        <TabsTrigger value="settings">Settings</TabsTrigger>
      </TabsList>
      <TabsContent value="overview">Overview content</TabsContent>
      <TabsContent value="settings">Settings content</TabsContent>
    </Tabs>
  )
}`}> 
    <Tabs defaultValue="overview">
      <TabsList>
        <TabsTrigger value="overview">Overview</TabsTrigger>
        <TabsTrigger value="settings">Settings</TabsTrigger>
      </TabsList>
      <TabsContent value="overview">Overview content</TabsContent>
      <TabsContent value="settings">Settings content</TabsContent>
    </Tabs>
  </ExampleBlock>

  ## Examples

  ### Glass

  <ExampleBlock code={`import { Tabs, TabsList, TabsTrigger, TabsContent } from "@glinui/ui"

export function TabsGlassDemo() {
  return (
    <Tabs defaultValue="first">
      <TabsList variant="glass">
        <TabsTrigger value="first" variant="glass">First</TabsTrigger>
        <TabsTrigger value="second" variant="glass">Second</TabsTrigger>
      </TabsList>
      <TabsContent value="first" variant="glass">First panel</TabsContent>
      <TabsContent value="second" variant="glass">Second panel</TabsContent>
    </Tabs>
  )
}`}> 
    <Tabs defaultValue="first">
      <TabsList variant="glass">
        <TabsTrigger value="first" variant="glass">First</TabsTrigger>
        <TabsTrigger value="second" variant="glass">Second</TabsTrigger>
      </TabsList>
      <TabsContent value="first" variant="glass">First panel</TabsContent>
      <TabsContent value="second" variant="glass">Second panel</TabsContent>
    </Tabs>
  </ExampleBlock>

  ### Liquid + Matte

  <ExampleBlock code={`import { Tabs, TabsList, TabsTrigger, TabsContent } from "@glinui/ui"

export function TabsSurfaceVariantsDemo() {
  return (
    <div className="grid gap-4 md:grid-cols-2">
      <Tabs defaultValue="liquid-1">
        <TabsList variant="liquid">
          <TabsTrigger value="liquid-1" variant="liquid">Liquid A</TabsTrigger>
          <TabsTrigger value="liquid-2" variant="liquid">Liquid B</TabsTrigger>
        </TabsList>
        <TabsContent value="liquid-1" variant="liquid">Liquid panel</TabsContent>
        <TabsContent value="liquid-2" variant="liquid">Second liquid panel</TabsContent>
      </Tabs>
      <Tabs defaultValue="matte-1">
        <TabsList variant="matte">
          <TabsTrigger value="matte-1" variant="matte">Matte A</TabsTrigger>
          <TabsTrigger value="matte-2" variant="matte">Matte B</TabsTrigger>
        </TabsList>
        <TabsContent value="matte-1" variant="matte">Matte panel</TabsContent>
        <TabsContent value="matte-2" variant="matte">Second matte panel</TabsContent>
      </Tabs>
    </div>
  )
}`}> 
    <div className="grid gap-4 md:grid-cols-2">
      <Tabs defaultValue="liquid-1">
        <TabsList variant="liquid">
          <TabsTrigger value="liquid-1" variant="liquid">Liquid A</TabsTrigger>
          <TabsTrigger value="liquid-2" variant="liquid">Liquid B</TabsTrigger>
        </TabsList>
        <TabsContent value="liquid-1" variant="liquid">Liquid panel</TabsContent>
        <TabsContent value="liquid-2" variant="liquid">Second liquid panel</TabsContent>
      </Tabs>
      <Tabs defaultValue="matte-1">
        <TabsList variant="matte">
          <TabsTrigger value="matte-1" variant="matte">Matte A</TabsTrigger>
          <TabsTrigger value="matte-2" variant="matte">Matte B</TabsTrigger>
        </TabsList>
        <TabsContent value="matte-1" variant="matte">Matte panel</TabsContent>
        <TabsContent value="matte-2" variant="matte">Second matte panel</TabsContent>
      </Tabs>
    </div>
  </ExampleBlock>

  ## Accessibility

  - Built on Radix Tabs with full keyboard nav.
  - Use clear trigger labels for each panel.

  ## Reduced Motion

  Tabs do not require motion for state communication.

  ## API Reference

  `Tabs` root follows Radix `Tabs.Root` props (`value`, `defaultValue`, `onValueChange`, `orientation`, `dir`).

  ### TabsList

  <PropsTable componentId="tabs" propsType="TabsListProps" autoAppendGenerated />

  ### TabsTrigger

  <PropsTable componentId="tabs" propsType="TabsTriggerProps" autoAppendGenerated />

  ### TabsContent

  <PropsTable componentId="tabs" propsType="TabsContentProps" autoAppendGenerated />

  ## Source

  ```tsx
  import { Tabs, TabsList, TabsTrigger, TabsContent } from "@glinui/ui"
  ```
</ComponentDocLayout>
