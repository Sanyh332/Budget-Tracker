import { getRegistryItem } from "@glinui/registry"
import { Textarea } from "@glinui/ui"
import { ComponentDocLayout } from "@/components/docs/component-doc-layout"
import { ExampleBlock } from "@/components/docs/example-block"
import { InstallTabs } from "@/components/docs/install-tabs"
import { getComponentSources } from "@/lib/get-component-source"
import { PropsTable } from "@/components/docs/props-table"
import { createDocsMetadata } from "@/lib/docs-metadata"

export const dynamic = "force-static"
export const item = getRegistryItem("textarea")
export const sources = getComponentSources(item?.files)
export const metadata = createDocsMetadata({
  title: "Textarea",
  description: item?.description ?? "Multi-line input with glass, liquid, matte, and utility variants.",
  path: "/docs/components/textarea"
})

<ComponentDocLayout
  badgeLabel="Primitive / Atom"
  componentId="textarea"
  title="Textarea"
  description={item?.description ?? "Multi-line input with glass, liquid, matte, and utility variants."}
>
  ## Installation

  ### Package Manager

  <InstallTabs sources={sources} command={item?.install.package ?? "pnpm add @glinui/ui @glinui/tokens"} />

  ### Registry

  <InstallTabs command={item?.install.registry ?? "pnpm dlx @glinui/cli@latest add textarea"} />

  ## Usage

  <ExampleBlock
    code={`import { Textarea } from "@glinui/ui"

export function TextareaBasicDemo() {
  return <Textarea aria-label="Basic textarea" placeholder="Write something..." />
}`}
  >
    <Textarea aria-label="Basic textarea" placeholder="Write something..." />
  </ExampleBlock>

  ## Examples

  ### With Placeholder

  <ExampleBlock
    code={`import { Textarea } from "@glinui/ui"

export function TextareaPlaceholderDemo() {
  return <Textarea aria-label="Issue details" placeholder="Describe your issue in detail..." />
}`}
  >
    <Textarea aria-label="Issue details" placeholder="Describe your issue in detail..." />
  </ExampleBlock>

  ### Custom Rows

  <ExampleBlock
    code={`import { Textarea } from "@glinui/ui"

export function TextareaRowsDemo() {
  return (
    <div className="space-y-3">
      <Textarea rows={2} aria-label="Small textarea" placeholder="2 rows" />
      <Textarea rows={4} aria-label="Default textarea" placeholder="4 rows (default)" />
      <Textarea rows={8} aria-label="Large textarea" placeholder="8 rows" />
    </div>
  )
}`}
  >
    <div className="space-y-3">
      <Textarea rows={2} aria-label="Small textarea" placeholder="2 rows" />
      <Textarea rows={4} aria-label="Default textarea" placeholder="4 rows (default)" />
      <Textarea rows={8} aria-label="Large textarea" placeholder="8 rows" />
    </div>
  </ExampleBlock>

  ### Surface Variants

  <ExampleBlock
    code={`import { Textarea } from "@glinui/ui"

export function TextareaVariantsDemo() {
  return (
    <div className="space-y-3">
      <Textarea variant="default" aria-label="Default textarea" placeholder="Default" />
      <Textarea variant="glass" aria-label="Glass textarea" placeholder="Glass" />
      <Textarea variant="liquid" aria-label="Liquid textarea" placeholder="Liquid" />
      <Textarea variant="matte" aria-label="Matte textarea" placeholder="Matte" />
      <Textarea variant="outline" aria-label="Outline textarea" placeholder="Outline" />
      <Textarea variant="filled" aria-label="Filled textarea" placeholder="Filled" />
    </div>
  )
}`}
  >
    <div className="space-y-3">
      <Textarea variant="default" aria-label="Default textarea" placeholder="Default" />
      <Textarea variant="glass" aria-label="Glass textarea" placeholder="Glass" />
      <Textarea variant="liquid" aria-label="Liquid textarea" placeholder="Liquid" />
      <Textarea variant="matte" aria-label="Matte textarea" placeholder="Matte" />
      <Textarea variant="outline" aria-label="Outline textarea" placeholder="Outline" />
      <Textarea variant="filled" aria-label="Filled textarea" placeholder="Filled" />
    </div>
  </ExampleBlock>

  ### Disabled

  <ExampleBlock
    code={`import { Textarea } from "@glinui/ui"

export function TextareaDisabledDemo() {
  return <Textarea aria-label="Disabled textarea" disabled placeholder="Cannot edit this field" />
}`}
  >
    <Textarea aria-label="Disabled textarea" disabled placeholder="Cannot edit this field" />
  </ExampleBlock>

  ## Accessibility

  - Always pair a `<Textarea>` with a `<label>` using `htmlFor` or supply an `aria-label`.
  - Placeholder text is not a substitute for labeling.
  - The `rows` attribute provides a visible size hint; use CSS `min-height` / `max-height` for precise control.
  - Disabled textareas convey their state through the native `disabled` attribute, readable by all major screen readers.

  ## Reduced Motion

  Textarea has no built-in animation. Focus ring and border transitions respect the user's `prefers-reduced-motion` setting automatically via Tailwind.

  ## API Reference

  <PropsTable componentId="textarea" autoAppendGenerated />

  ## Source

  ```tsx
  import { Textarea } from "@glinui/ui"
  ```

  {item?.files?.map((file) => (
    <div key={file} className="font-mono text-sm text-neutral-600 dark:text-neutral-300">
      {file}
    </div>
  ))}
</ComponentDocLayout>
